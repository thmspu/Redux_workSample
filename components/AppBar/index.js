import AppBar from './AppBar'
// import { injectReducer } from '../../store/reducers'

/**
export const initAppBar = (store) => {
  const reducer = require('./reducer').default
  injectReducer(store, { key: 'appbar', reducer })
}
 */
export default AppBar
